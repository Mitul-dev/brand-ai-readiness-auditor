# Runtime dependencies (deliberately small: no agent frameworks, no model weights).
httpx>=0.27,<0.29
beautifulsoup4>=4.12,<5
lxml>=5.0
Flask>=3.0,<4

# Optional: enables the raw-HTML vs rendered-DOM comparison.
# Install the browser with:  python -m playwright install chromium
playwright>=1.44

# Development / testing only.
pytest>=8.0